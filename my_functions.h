#include <Windows.h>
#include <stdio.h>
#include <string.h>

void error_message(DWORD errorCode, const char* error_type, const char* error_detail, const char* counter_measure);
void error_message(DWORD errorCode, const char* error_type, const char* counter_measure);
void error_message(const char* error_type, const char* error_detail, const char* counter_measure);
void error_message(const char* error_type, const char* counter_measure);
void error_message(const char* error_type);